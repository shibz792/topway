import { notFound } from "next/navigation";
import { industries } from "@/content/site";
import { PageHero } from "@/components/ui";
import { ConversionJourney, DecisionSection, EmployerCTA, FAQSection, ListSection, ProofBar } from "@/components/page-sections";
export function generateStaticParams(){return industries.map(x=>({slug:x.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const x=industries.find(i=>i.slug===slug);return {title:x?.name||"Industry",description:x?.summary}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const x=industries.find(i=>i.slug===slug);if(!x)notFound();return <><PageHero eyebrow="Industries" title={`${x.name} recruitment from Sri Lanka`} copy={x.summary}/><ProofBar/><DecisionSection eyebrow="Recruitment approach" title={`Build a practical ${x.name.toLowerCase()} workforce plan.`} copy="Topway aligns sourcing and screening with the roles, experience and operational expectations confirmed by the employer." items={[["Define the requirement",`Confirm responsibilities, criteria and workforce numbers for ${x.name.toLowerCase()}.`],["Screen for the role",x.screening],["Make the selection","Coordinate relevant interviews and employer decisions."]]}/><ListSection eyebrow="Example positions" title="Roles sourced against confirmed requirements" items={x.roles}/><ConversionJourney/><FAQSection items={[["Can Topway source roles not shown here?","Yes. Share the specific requirement so Topway can assess sourcing feasibility and the appropriate screening approach."],["How are candidates screened?","Screening is aligned with the confirmed role, experience, documents and any relevant practical assessment."],["Can we recruit multiple role types?","Yes. A campaign can be organised around several roles and workforce categories."]]}/><EmployerCTA/></>}
