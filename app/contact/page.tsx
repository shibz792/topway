import { EnquiryForm } from "@/components/forms";
import { PageHero, SectionHead } from "@/components/ui";
import { contact } from "@/content/site";
import { ProofBar } from "@/components/page-sections";
export const metadata={title:"Contact",description:"Contact Topway Private Limited in Colombo, Sri Lanka."};
export default function Page(){return <><PageHero eyebrow="Contact" title="Start the right conversation with Topway." copy="Tell us whether you are planning a workforce requirement, registering candidate interest or verifying company information."/><ProofBar/><section className="conversion-form-section"><div className="container conversion-form-grid"><div className="contact-details"><span className="eyebrow">Contact office</span><h2>Speak directly with our team.</h2><p>Use the enquiry form for a structured response, or contact Topway directly for current information.</p><div><strong>Office</strong><p>{contact.address.map(x=><span key={x}>{x}<br/></span>)}</p></div><div><strong>Chairman — Azzam Muhammedh</strong><a href={`tel:${contact.mobile}`}>{contact.mobileDisplay}</a></div><div><strong>Office lines</strong>{contact.office.map(x=><p key={x}>{x}</p>)}</div><div><strong>Email</strong><a href={`mailto:${contact.email}`}>{contact.email}</a></div><a className="btn btn-primary" href={`https://wa.me/${contact.mobile.replace("+","")}`}>WhatsApp Topway</a></div><div className="form-shell"><h3>Send an enquiry</h3><p className="muted">Share enough detail for our team to route your enquiry correctly.</p><EnquiryForm/></div></div></section></>}
