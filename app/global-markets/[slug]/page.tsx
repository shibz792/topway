import { notFound } from "next/navigation";
import { markets, availability } from "@/content/site";
import { Disclaimer, PageHero } from "@/components/ui";
import { ConversionJourney, DecisionSection, EmployerCTA, ListSection, ProofBar } from "@/components/page-sections";
export function generateStaticParams(){return markets.map(x=>({slug:x.slug}))}
export async function generateMetadata({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const x=markets.find(i=>i.slug===slug);return {title:`${x?.name||"Market"} Recruitment`,description:x?.summary}}
export default async function Page({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const x=markets.find(i=>i.slug===slug);if(!x)notFound();return <><PageHero eyebrow={`${x.status} · ${x.region}`} title={`Recruitment enquiries for ${x.name}`} copy={x.summary}/><ProofBar/><section className="section-sm"><div className="container"><Disclaimer/></div></section><DecisionSection eyebrow="Before recruitment begins" title={`Confirm the current position for ${x.name}.`} copy={availability} items={[["Confirm employer demand","Verify current requirements and relevant worker categories."],["Review eligibility","Check the role, documentation and country-specific conditions."],["Proceed on confirmation","Begin sourcing or candidate processing only against a confirmed requirement."]]}/><ListSection eyebrow="Common industries" title={`Potential workforce requirements in ${x.name}`} items={x.industries}/><ListSection eyebrow="Relevant roles" title="Worker categories for confirmed requirements" items={x.roles}/><ConversionJourney/><EmployerCTA/></>}
